#!/usr/bin/env python3
from flet import app, Text, Column, Row, Page, View, AppBar, ElevatedButton, colors


def logged_view(page):
    page.clean()
    return view

def login_view(page):
    page.clean()
    view = _view(page,'login_view', 'LOGIN')
    return view

views = {
    "/login": login_view,
    "/home": logged_view
}

actual_view = '/login'

def app_loop(page: Page):
    
app(target=app_loop)

